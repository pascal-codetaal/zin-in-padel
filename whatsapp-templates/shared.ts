/** Footer on cascade invite messages (opt-out hint). */
export const INVITE_STOP_FOOTER =
  "Stuur STOP om geen uitnodigingen meer te ontvangen.";

/** Meta/WhatsApp CTA button labels (≤20 chars, no emoji). */
export const INVITE_ACCEPT_BUTTON_LABEL = "Ja, ik doe mee";
export const INVITE_DECLINE_BUTTON_LABEL = "Nee, andere keer";

/** Plain-text invite lines (session messages until Content templates are wired). */
export const INVITE_ACCEPT_LINE_PREFIX = `✅ ${INVITE_ACCEPT_BUTTON_LABEL}:`;
export const INVITE_DECLINE_LINE_PREFIX = `❌ ${INVITE_DECLINE_BUTTON_LABEL}:`;
